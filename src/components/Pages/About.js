import React from 'react'

function About() {


  return (
    //we use fragment instead of div if we dont necessarily want a div
    <React.Fragment>
        <h1>About</h1>
        <p>This is a TodoList app in React built to learn React/Router basics.</p>
      
    </React.Fragment>
  )
}

export default About;
